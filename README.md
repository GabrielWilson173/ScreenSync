# ScreenSync



Built using React

npm install -g react-scripts



npm start -> https://localhost:3000


To run the FASTAPI API, run

fastapi dev -> http://localhost:8000

To deploy the backend server, do:
cloudflared tunnel --url http://localhost:8000

and place the url in ./Frontend/config.js